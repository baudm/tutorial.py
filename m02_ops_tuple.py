#!/usr/bin/env python3

xyz_coordinates = (3, 0, -1)

# Unpack values
x, y, z = xyz_coordinates
# or (x, y, z) = xyz_coordinates if you want to be more verbose (please don't)

print('x:', x, 'y:', y, 'z:', z)
